import React from "react";

// PLEASE NOTE THAT : if i replace the folder name from _lib to %5flib then type routr /%5flib it will work and i will see the content (event though the url will change to _lib on hitting enter) but if i keep the folder name as %5Flib and then try to type the route as /%5Flib it will not work and it will show 404 page not found

export default function PrivatePage() {
  return <div>Private Page</div>;
}
